name: CI / Build / Publish

on:
  push:
    branches: [main]
    tags: ["*"]
  workflow_dispatch: {}

jobs:
  test:
    name: Test across Python versions
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.10", "3.11"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python }}

      - name: Install uv
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade uv

      - name: Install project and test deps (via uv)
        run: |
          uv install || python -m pip install -e '.[dev]' || python -m pip install -e .

      - name: Run tests (via uv)
        run: uv run pytest -q

  publish:
    name: Build and Publish (single runner)
    runs-on: ubuntu-latest
    needs: test
    # only run publish once (not per-matrix) after tests succeed
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Install uv
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade uv

      - name: Determine version
        id: version
        run: |
          set -e
          echo "GITHUB_REF=${GITHUB_REF}"
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            TAG=${GITHUB_REF#refs/tags/}
            # strip a leading 'v' if present
            TAG=${TAG#v}
            echo "version=$TAG" >> "$GITHUB_OUTPUT"
            echo "tagged=true" >> "$GITHUB_OUTPUT"
          else
            git fetch --tags --prune
            LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
            # strip leading 'v' from latest tag if present
            LATEST_TAG=${LATEST_TAG#v}
            SHORT_SHA=$(git rev-parse --short=7 HEAD)
            # Use a PEP 440-compliant dev version: <base>.dev0+<sha>
            DEV_VERSION="${LATEST_TAG}.dev0+${SHORT_SHA}"
            echo "version=$DEV_VERSION" >> "$GITHUB_OUTPUT"
            echo "tagged=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Patch pyproject for dev version
        if: steps.version.outputs.tagged == 'false'
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          echo "Patching pyproject.toml to version=${VERSION}"
          sed -E -i.bak "s/^version[[:space:]]*=[[:space:]]*\".*\"/version = \"${VERSION}\"/" pyproject.toml
          echo "Patched pyproject.toml:"
          sed -n '1,120p' pyproject.toml

      - name: Build distribution (via uv)
        run: |
          uv build

      - name: Publish to TestPyPI (via uv)
        if: steps.version.outputs.tagged == 'false'
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: |
          set -e
          uv publish --publish-url https://test.pypi.org/legacy/

      - name: Publish to PyPI (via uv)
        if: steps.version.outputs.tagged == 'true'
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          set -e
          uv publish
